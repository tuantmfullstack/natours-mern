export const loginSelector = (state) => state.auth.isLogin;
export const toursSelector = (state) => state.tours.tours;
export const tourSelector = (state) => state.tours.tour;
export const loadingTour = (state) => state.tours.isLoading;
export const loading = (state) => state.auth.isLoading;
